/**
 * NestJS Backend Extractor Plugin (Reserved for future backend support phase)
 *
 * This placeholder satisfies the Phase 1 plugin architecture contract.
 * NestJS extractors will plug directly into this module during future phases.
 */
export function extractNestControllers(_ast) {
  return [];
}
